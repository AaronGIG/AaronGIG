global env_var hvar数字1 hvar数字2 hvar数字3 //填写需要调用的对应变量字符串(此处是字符串 不加引号！！！)
*变量分别保留文件
local i = 1
foreach v in $env_var{
preserve
capture keep if var == "`v'" //批量调用字符串
rename (hvar) (`v')
drop var
save "~/`v'.dta", replace
restore
local i = `i' + 1
}
